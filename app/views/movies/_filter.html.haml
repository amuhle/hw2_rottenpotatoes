= form_tag movies_path, :method => :get do
  Include:
  - @all_ratings.each do |rating|
    = rating
    - value =  @ratings_keys.include?(rating)
    = check_box_tag "ratings[#{rating}]", value, value
  = submit_tag 'Refresh'
